{
  "id": "reef",
  "name": "Reef",
  "description": "Swarm observability dashboard — monitor agent activity, inspect session traces, and visualize delegation trees.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true
      },
      "port": {
        "type": "number",
        "default": 3179,
        "minimum": 1,
        "maximum": 65535
      },
      "host": {
        "type": "string",
        "default": "127.0.0.1",
        "description": "Bind address. Use 0.0.0.0 to allow remote access."
      },
      "dataDir": {
        "type": "string",
        "description": "Path to the OpenClaw data directory. Defaults to ~/.openclaw (stateDir)."
      }
    }
  },
  "uiHints": {
    "port": {
      "label": "Dashboard Port",
      "help": "HTTP port for the Reef web dashboard",
      "placeholder": "3179"
    },
    "host": {
      "label": "Bind Address",
      "help": "Network interface to bind to. 127.0.0.1 = local only, 0.0.0.0 = all interfaces.",
      "advanced": true,
      "placeholder": "127.0.0.1"
    },
    "dataDir": {
      "label": "Data Directory",
      "help": "Path to the OpenClaw data directory containing agents/. Defaults to ~/.openclaw.",
      "advanced": true
    }
  }
}
